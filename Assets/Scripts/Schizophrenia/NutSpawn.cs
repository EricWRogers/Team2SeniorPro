using UnityEngine;

public class NutSpawn : MonoBehaviour
{
    //kaden made this but basically you pick up wrong nut and all of the instantiated one dissapeared.  If they dont touch, they dissapear on their own, but if you touch you get sanity drop.
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
