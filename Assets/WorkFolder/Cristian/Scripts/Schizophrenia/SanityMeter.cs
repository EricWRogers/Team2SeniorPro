using UnityEngine;

public class SanityMeter : MonoBehaviour
{

    //in this, the meter only becomes un hidden if you run into a trap that gives you the debuff, same with health, this cna be like the games with a breathing system
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
